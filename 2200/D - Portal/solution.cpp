#include <bits/stdc++.h>using namespace std; // macros#define ll long long#define yes cout << "YES\n"#define no cout << "NO\n"#define f(i, size) for (int i = 0; i < size; i++)// #define ia(a, n) \//     int a[n];     \//     f(i, n) cin >> a[i] #define iv(v, n)     \    vector<ll> v(n); \    f(i, n) cin >> v[i] // #define M (1000000007)#define INF 1000000000000000000LL#define mp make_pair#define mt make_tuple #define nline '\n'#define pb push_back#define pii pair<int, int>#define pll pair<ll, ll>#define vii vector<pair<int, int>> void solve() {    ll n,x,y;    cin>>n>>x>>y;    iv(a,n);    vector<ll>mid;    vector<ll>leftright;    for(int i=0; i<x; i++) leftright.pb(a[i]);    for(int i=x; i<y; i++) mid.pb(a[i]);    for(int i=y; i<n; i++) leftright.pb(a[i]);      auto mn=min_element(mid.begin(), mid.end());    rotate(mid.begin(), mn, mid.end());    ll idx=leftright.size();    for(int i=0; i<leftright.size(); i++){        if(leftright[i]>mid.front()){            idx=i;            break;        }    }     // vector<ll>ans;    for(int i=0; i<idx; i++) cout<<leftright[i]<<" ";    for(int i=0; i<mid.size(); i++) cout<<mid[i]<<" ";    for(int i=idx; i<leftright.size(); i++) cout<<leftright[i]<<" ";    // for(int i=0; i<ans.size(); i++) cout<<ans[i]<<" ";    cout<<nline;} int main(){    ios::sync_with_stdio(false);    cin.tie(nullptr);    cout.tie(nullptr);     ll t;    cin >> t;    while (t--) {        solve();    }    return 0;}